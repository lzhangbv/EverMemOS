"""
Knowledge Graph Extraction Prompts - Chinese Version

从记忆原子事实中抽取实体关系三元组、事件实体关联、事件间关系的提示词。
适配自 AutoSchemaKG 的 TRIPLE_INSTRUCTIONS。
"""

KG_SYSTEM_PROMPT = (
    "你是一个始终以有效JSON数组格式回应的助手，不要输出任何解释，只输出JSON数组。"
)

KG_ENTITY_RELATION_PROMPT = """给定一组来自对话记忆的原子事实，提取所有重要实体及其关系。

规则：
- 实体必须是具体名词（人名、地名、组织、物品、概念），排除代词。
- 关系应为简洁的动词或动词短语，清晰表达实体间的联系。
- 关系描述不应重复头尾实体的信息。
- 每个三元组必须独立且有意义。

返回格式必须为以下JSON结构：
[
    {
        "Head": "{具体名词}",
        "Relation": "{动词或关系描述}",
        "Tail": "{具体名词}"
    }
]

如果无法提取有意义的实体关系，返回空数组：[]

以下是原子事实：
"""

KG_EVENT_ENTITY_PROMPT = """给定一组来自对话记忆的原子事实，识别事件及其参与实体。

规则：
- 每个事件应为独立单句，描述发生了什么。
- 列出参与每个事件的所有实体（人物、地点、物品）。
- 实体必须是具体名词，不含代词。
- 不要使用省略号。

返回格式必须为以下JSON结构：
[
    {
        "Event": "{描述事件的简单句子}",
        "Entity": ["实体1", "实体2"]
    }
]

如果无法提取有意义的事件，返回空数组：[]

以下是原子事实：
"""

KG_EVENT_RELATION_PROMPT = """给定一组来自对话记忆的原子事实，识别事件间的时序和因果关系。

规则：
- 每个事件应为独立单句。
- 关系类型：之前、之后、同时、因为、结果
- 每个三元组必须具体、有意义且能独立理解。
- 不要使用省略号。

返回格式必须为以下JSON结构：
[
    {
        "Head": "{事件1描述}",
        "Relation": "{时序或因果关系}",
        "Tail": "{事件2描述}"
    }
]

如果无法提取有意义的事件关系，返回空数组：[]

以下是原子事实：
"""
